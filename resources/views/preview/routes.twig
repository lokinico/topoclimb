{% extends "layouts/app.twig" %}

{% block title %}Aperçu des voies - TopoclimbCH{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    
    <!-- Header avec call-to-action -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="alert alert-warning d-flex align-items-center" role="alert">
                <div class="flex-grow-1">
                    <h5 class="alert-heading">🧗 Aperçu limité - Voies d'escalade</h5>
                    <p class="mb-0">
                        Seules quelques voies sont visibles. 
                        <strong>Des centaines d'autres</strong> avec cotations, longueurs et topos détaillés !
                    </p>
                </div>
                <div class="ms-3">
                    <a href="{{ register_url }}" class="btn btn-success">✨ Accès complet</a>
                    <a href="{{ login_url }}" class="btn btn-outline-primary ms-2">🔑 Connexion</a>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-8">
            <h2>🧗 Voies d'exemple</h2>
            
            {% if routes %}
            <div class="row">
                {% for route in routes %}
                <div class="col-md-6 mb-4">
                    <div class="card h-100 position-relative">
                        {% if route.is_preview %}
                        <div class="position-absolute top-0 end-0 m-2">
                            <span class="badge bg-info">👀 Échantillon</span>
                        </div>
                        {% endif %}
                        
                        <div class="card-body">
                            <h6 class="card-title">
                                🧗 {{ route.name }}
                            </h6>
                            
                            <div class="mb-3">
                                <span class="badge bg-primary me-2">{{ route.difficulty }}</span>
                                {% if route.sector_name %}
                                <small class="text-muted">{{ route.sector_name }}</small>
                                {% endif %}
                                {% if route.region_name %}
                                <small class="text-muted"> • {{ route.region_name }}</small>
                                {% endif %}
                            </div>
                            
                            <!-- Informations masquées -->
                            <div class="bg-light p-2 rounded mb-3 text-muted">
                                <small class="fw-bold">🔒 Détails membres uniquement :</small><br>
                                <small>
                                    • Longueur exacte et nombre de points<br>
                                    • Description détaillée du parcours<br>
                                    • Coordonnées GPS de départ<br>
                                    • Photos et topos<br>
                                    • Commentaires et conseils
                                </small>
                            </div>
                            
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group btn-group-sm">
                                    <button class="btn btn-outline-secondary" disabled title="Membres uniquement">
                                        📸 Photos
                                    </button>
                                    <button class="btn btn-outline-secondary" disabled title="Membres uniquement">
                                        🗺️ Localiser
                                    </button>
                                    <button class="btn btn-outline-secondary" disabled title="Membres uniquement">
                                        ⭐ Noter
                                    </button>
                                </div>
                                
                                <button class="btn btn-outline-danger btn-sm" disabled title="Membres uniquement">
                                    ❤️ Favoris
                                </button>
                            </div>
                        </div>
                        
                        <div class="card-footer bg-light">
                            <div class="text-center">
                                <small class="text-muted">
                                    <a href="{{ register_url }}" class="text-success fw-bold">
                                        S'inscrire pour voir tous les détails
                                    </a>
                                </small>
                            </div>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
            
            <!-- Message pour plus de contenu -->
            <div class="alert alert-secondary text-center">
                <h5>🚀 {{ total_hidden }}</h5>
                <p>Des voies de tous niveaux, du débutant au expert, dans toute la Suisse.</p>
                <a href="{{ register_url }}" class="btn btn-success">Voir toutes les voies</a>
            </div>
            
            {% else %}
            <div class="card">
                <div class="card-body text-center">
                    <h5>Aucune voie disponible pour l'aperçu</h5>
                    {% if error %}
                    <p class="text-muted">{{ error }}</p>
                    {% endif %}
                    <a href="{{ register_url }}" class="btn btn-primary">
                        S'inscrire pour voir le contenu complet
                    </a>
                </div>
            </div>
            {% endif %}
        </div>
        
        <div class="col-lg-4">
            <!-- Sidebar avec avantages membre -->
            <div class="card mb-4">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0">🎁 Avantages membre</h5>
                </div>
                <div class="card-body">
                    <ul class="list-unstyled">
                        <li class="mb-2">
                            <span class="text-success">✓</span> 
                            <strong>Toutes les voies</strong><br>
                            <small class="text-muted">Accès à la base complète</small>
                        </li>
                        <li class="mb-2">
                            <span class="text-success">✓</span> 
                            <strong>Coordonnées GPS</strong><br>
                            <small class="text-muted">Navigation précise vers chaque voie</small>
                        </li>
                        <li class="mb-2">
                            <span class="text-success">✓</span> 
                            <strong>Photos et topos</strong><br>
                            <small class="text-muted">Visualisation complète</small>
                        </li>
                        <li class="mb-2">
                            <span class="text-success">✓</span> 
                            <strong>Commentaires</strong><br>
                            <small class="text-muted">Conseils de la communauté</small>
                        </li>
                        <li class="mb-2">
                            <span class="text-success">✓</span> 
                            <strong>Favoris et planning</strong><br>
                            <small class="text-muted">Organisez vos sorties</small>
                        </li>
                        <li class="mb-2">
                            <span class="text-success">✓</span> 
                            <strong>Météo temps réel</strong><br>
                            <small class="text-muted">Conditions actuelles</small>
                        </li>
                    </ul>
                    
                    <div class="d-grid gap-2 mt-3">
                        <a href="{{ register_url }}" class="btn btn-success">
                            ✨ Inscription gratuite
                        </a>
                        <a href="{{ login_url }}" class="btn btn-outline-secondary">
                            🔑 Se connecter
                        </a>
                    </div>
                </div>
            </div>
            
            <!-- Niveaux de difficulté -->
            <div class="card">
                <div class="card-header">
                    <h6 class="mb-0">📊 Niveaux disponibles</h6>
                </div>
                <div class="card-body">
                    <div class="row g-2">
                        <div class="col-6">
                            <span class="badge bg-success w-100">3a - 4c</span>
                            <small class="text-muted d-block">Débutant</small>
                        </div>
                        <div class="col-6">
                            <span class="badge bg-primary w-100">5a - 6b</span>
                            <small class="text-muted d-block">Intermédiaire</small>
                        </div>
                        <div class="col-6">
                            <span class="badge bg-warning w-100">6c - 7b</span>
                            <small class="text-muted d-block">Avancé</small>
                        </div>
                        <div class="col-6">
                            <span class="badge bg-danger w-100">7c+</span>
                            <small class="text-muted d-block">Expert</small>
                        </div>
                    </div>
                    
                    <div class="mt-3 p-2 bg-light rounded">
                        <small class="text-muted">
                            🔒 Filtres avancés par niveau, longueur, type de voie disponibles pour les membres
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Footer navigation -->
    <div class="row mt-5">
        <div class="col-12">
            <nav aria-label="Navigation aperçu">
                <ul class="pagination justify-content-center">
                    <li class="page-item">
                        <a class="page-link" href="/preview">← Aperçu principal</a>
                    </li>
                    <li class="page-item active">
                        <span class="page-link">Voies d'escalade</span>
                    </li>
                    <li class="page-item">
                        <a class="page-link" href="/preview/sectors">Secteurs →</a>
                    </li>
                </ul>
            </nav>
        </div>
    </div>
</div>
{% endblock %}

{% block styles %}
<style>
.card.position-relative .badge {
    z-index: 10;
}

.btn:disabled {
    cursor: not-allowed;
    opacity: 0.5;
}

.bg-light.rounded {
    border-left: 3px solid #17a2b8;
}

.badge.w-100 {
    padding: 0.5rem;
}
</style>
{% endblock %}